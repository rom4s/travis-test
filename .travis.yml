branches:
  only:
    - master

sudo: required

git:
  depth: 10

matrix:
  include:
    - os : linux
      dist: trusty
      language: cpp
      python: "3.8"

    - os : windows
      language : cpp
      before_install:
        - choco install python --version=3.8.0
        - export PATH=/C/Python38:$PATH


script: python3 --version; python --version; echo ~